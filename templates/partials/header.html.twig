<nav class="navbar navbar-expand-md bg-white border-bottom shadow-sm sticky-top">
    <div class="container">
        <a class="navbar-brand fw-bold text-primary" href="{{ path('app_home') }}">
            <i class="bi bi-journal-richtext me-1"></i> Mini Blog
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav me-auto mb-2 mb-md-0 gap-1">
                <li class="nav-item">
                    <a href="{{ path('app_home') }}" class="nav-link rounded-3 px-3">
                        <i class="bi bi-house-door me-1"></i> Home
                    </a>
                </li>
                {# <li class="nav-item"><a href="{{ path('app_category') }}" class="nav-link rounded-3 px-3">Categories</a></li> #}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a href="{{ path('app_admin') }}" class="nav-link rounded-3 px-3">
                            <i class="bi bi-speedometer2 me-1"></i> Admin
                        </a>
                    </li>
                {% endif %}
            </ul>

            <div class="d-flex align-items-center gap-2">
                {% if app.user %}
                    <span class="text-body-secondary small">
                        <i class="bi bi-person-circle me-1"></i>Hello <strong>{{ app.user.firstName }}</strong>
                    </span>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm rounded-3 px-3">
                        <i class="bi bi-box-arrow-right me-1"></i> Logout
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-primary btn-sm rounded-3 px-3">Login</a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary btn-sm rounded-3 px-3">Register</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>